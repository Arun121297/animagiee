import 'package:flutter/material.dart';

class Search_UI extends StatelessWidget {
  const Search_UI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return IconButton(
        onPressed: () {},
        icon: Icon(
          Icons.search,
          color: Colors.black,
        ));
  }
}
